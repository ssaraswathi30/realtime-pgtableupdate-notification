{
  "name": "pg-notify-listener",
  "version": "1.0.0",
  "description": "Listens to PostgreSQL pg_notify and broadcasts via Socket.IO",
  "main": "listener.js",
  "dependencies": {
    "pg": "^8.11.3",
    "socket.io": "^4.7.5"
  },
  "scripts": {
    "start": "node listener.js"
  }
}
